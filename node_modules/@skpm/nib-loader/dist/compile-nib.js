'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = compileNib;

var _child_process = require('child_process');

function compileNib(loader, nibPath, cb) {
  // Tell webpack to cache the compiled file and to track changes to the source files
  if (loader.cacheable) {
    loader.cacheable();
    loader.addDependency(loader.resourcePath);
  }

  (0, _child_process.execFile)('/usr/bin/ibtool', ['--compile', nibPath, loader.resourcePath], { encoding: 'utf8' }, cb);
}