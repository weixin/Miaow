{
  "name": "miaow",
  "version": "1.4.4",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "miaow",
    "manifest": "src/manifest.json",
    "main": "miaow.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.2"
  },
  "author": "doubleluo <doubleluo@tencent.com>",
  "dependencies": {
    "@skpm/events": "^0.1.1",
    "@skpm/fs": "^0.1.2",
    "@skpm/path": "^0.1.0",
    "babel-polyfill": "^6.26.0",
    "js-yaml": "^3.11.0",
    "mocha-js-delegate": "^0.1.1",
    "sketch-module-web-view": "git://github.com/romannurik/sketch-module-wk-web-view.git"
  }
}
